export interface AppSettings {
  appName: string;
  defaultLocale: string;
  supportEmail: string;
}

export const appSettings: AppSettings = {
  appName: 'EduSpell Pro AI',
  defaultLocale: 'en',
  supportEmail: 'support@eduspell.ai',
};
